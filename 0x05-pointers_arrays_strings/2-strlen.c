#include "main.h"
/**
 *_strlen -  returns the length of a string.
 *@s: the string.
 *Return:the length 
 *
 *
 *
 *
 *
 *
 */
int _strlen(char *s)
{
	int count,i;
	char c[] = *s;

	for(i = 0; c[i] != '\0'; i++)
	{
		count = i;
	}
	return count;
}
