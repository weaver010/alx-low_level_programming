#include "main.h"
/**
 *_strlen -  returns the length of a string.
 *@s: the string.
 *Return:the length 
 *
 *
 *
 *
 *
 *
 */
int _strlen(char *s)
{
	int count,i;

	for(i = 0; s[i] != '\0'; i++)
	{
		count = i + 1;
	}
	return count;
}
