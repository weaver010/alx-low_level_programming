#include "main.h"
/**
 * _abs - return absolute value for input number
 * @n : data type of input
 * Return: the absolute value
 *
 *
 *
 *
 *
 */
int _abs(int n)
{

	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
